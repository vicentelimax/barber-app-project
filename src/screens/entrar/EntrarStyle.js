import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin:'7%',
    },
    button: {
        margin:'4%',
        padding:2
    },
    containerText: {
        display: 'flex',
        flexDirection:'row',
        aligItems:'center'
    }
  });

export default styles;